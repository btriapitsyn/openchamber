import React from 'react';
import { SectionPlaceholder } from '../SectionPlaceholder';

export const SettingsSidebar: React.FC = () => {
    return <SectionPlaceholder sectionId="settings" variant="sidebar" />;
};
