import React from 'react';
import { SectionPlaceholder } from '../SectionPlaceholder';

export const SettingsPage: React.FC = () => {
    return <SectionPlaceholder sectionId="settings" variant="page" />;
};
